(CodingTest) : 12일차 10000000을 1,000,000로 변환

class Solution {
    public String solution(String s) {

        StringBuilder sb = new StringBuilder(s);

        int len = s.length(); // 예시 7

        for(int i = len-3 ; i > 0; i-=3){ // i=4, 4<7 ,, i=1, 1 < 7
                                            // 1,000,000.
                                            // i=1, i=4 ...
        sb.insert(i, ",");

        }

    return sb.toString();
    }
}