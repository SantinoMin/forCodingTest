# out의 DATETIME이 ins의 DATETIME보다 빠른 경우
# ANIMAL_ID, NAME 출력
# DATETIME이 빠른순으로 정렬

SELECT i.ANIMAL_ID, i.NAME
FROM ANIMAL_INS i
JOIN ANIMAL_OUTS o
ON i.ANIMAL_ID = o.ANIMAL_ID
WHERE i.DATETIME > o.DATETIME
ORDER BY i.DATETIME ASC;