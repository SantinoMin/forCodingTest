class Solution {
    public int solution(int n) {
        int answer = 0;

        // n 이하의 짝수를 더하기
        for (int i = 2; i <= n; i += 2) {
            answer += i;
        }

        return answer;
    }
}